[build-system]
requires = ['setuptools>=61.0', 'wheel']
build-backend = 'setuptools.build_meta'

[project]
name = "pg-macros"
version = "0.1.0"
description = "PG macros package"
requires-python = ">=3.12"
dependencies = [
    "pg-math>=0.1.0",
    "pg-parser>=0.1.0",
]
